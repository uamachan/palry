# Pairly Project Rules

このリポジトリを変更するAI/開発者は、以下を必ず守ること。

## 最重要ルール

- 既存ファイルを雑に全面上書きしない。
- 関係ないファイルをまとめて変更しない。
- 変更は目的に必要な最小差分にする。
- main へ直接反映する場合でも、変更内容を明確に説明する。
- `.env`、秘密鍵、Firebase Admin秘密情報、APIシークレットは絶対にコミットしない。

## CSS / UI 修正ルール

- 後入れ JavaScript で CSS を注入しない。
  - 例: `document.createElement('style')`、`style.textContent = css`、`appendChild(style)` でレイアウトを直す方法は禁止。
  - 一時的な `*-final.js`、`*-hotfix.js` でCSSを上書きする方式も禁止。
- UI を直す時は、必ず元のCSSまたは既存の責務に合うCSSファイルを編集する。
- 新しい上書き専用CSSファイルを安易に追加しない。
- `!important` は原則使わない。既存の強い上書きを整理する目的で必要な場合のみ、理由が分かる範囲で最小限にする。
- スマホUIだけを直す場合は、`@media (max-width: 760px)` の中で限定して直す。
- PC表示に影響する変更を混ぜない。

## スマホUIの基本方針

- 一般的なモバイルWeb/アプリのフォーマットに寄せる。
- ランディングページは普通に縦スクロールできる状態を維持する。
- 料金表・安全/規約・フッターがスマホで最後まで見えることを必ず確認する。
- ログイン後のアプリ画面は、必要な箇所だけ内部スクロールにする。
- `html`、`body`、`#root` に `overflow: hidden` や `height: 100%` を当てる場合は、ランディングページに副作用が出ないようにスコープを分ける。
- ヘッダーは基本的に、ロゴ左・ログイン/アカウント操作右・ナビは下段または横スクロール可能な行にする。

## 既存の後付けCSS/JSについて

- 既にある public 側の hotfix CSS は、可能なら元の `client/src` 側CSSへ段階的に統合する。
- `landing-mobile-final.js` のような後入れCSS注入ファイルは使わない。
- 既存の後付けCSSを消す時は、画面崩れを確認しながら1ファイルずつ行う。

## 確認項目

UI変更後は最低限これを確認する。

1. スマホ幅でトップからフッターまで縦スクロールできる。
2. 料金表が最後まで見える。
3. 安全・規約が最後まで見える。
4. ロゴ、ログイン/登録、ナビが被らない。
5. ログイン後のマッチング画面、DM、通知、プロフィール画面のスクロールが壊れていない。
6. PC表示が崩れていない。

## 推奨コマンド

変更後は可能な限り以下を実行する。

```bash
npm test
npm run build
npm run release:check
```
